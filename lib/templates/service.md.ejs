---
<%=stopText%>
sidebar: auto
---


# <%= service.nameCamel%>

::: tip Description
<%= service.docs.description%>
:::

<% if(service.apiDocs) {%>
  ## Properties
| Name | Type |
| ---- | ---- |
<% service.apiDocs.filter(function(x){
  return x.meta !== undefined && 
  x.meta.code.type != 'FunctionDeclaration' && 
  x.meta.code.type != null &&
  x.meta.code.name != '_' }).forEach(function(apiDocs){ -%>
| <%=apiDocs.meta.code.name%> | <%=apiDocs.meta.code.type%> |
<% }); %>
<% } %>

<% if(service.schema) {%>
## Schema
<%=service.schema%>

<% } %>

<% if(service.bootBefore) {%>
  ## Boot Before
  <%=service.bootBefore%>
<% } %>

<% if(service.bootAfter) {%>
  ## Boot After
  <%=service.bootAfter%>
<% } %>

<% if(service.refProperties) {%>
  ## Ref Properties
  <%=service.refProperties%>
<% } %>

<% service.apiDocs.filter(function(x){return x.meta !== undefined && x.meta.code.type != 'FunctionDeclaration' && x.meta.code.type != ''})%>